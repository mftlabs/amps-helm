apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{.Release.Name}}-cfg-pvc
spec:
  storageClassName: {{.Values.cfg.storageClassName}}
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{.Values.cfg.size}}