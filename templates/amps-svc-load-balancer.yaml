apiVersion: v1
kind: Service
metadata:
  name: amps-svc-ui
  labels:
    name: amps
    instance: amps-svc-load-balancer
spec:
  type: LoadBalancer
  ports: 
{{ toYaml .Values.ports| indent 4 }}
  selector:
    name: amps
    instance: amps-node

